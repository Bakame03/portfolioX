{
  "name": "portfolio-voice",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@google/genai": "^2.24.0"
  },
  "devDependencies": {
    "wrangler": "^4.140.0"
  }
}
